diff --git i/CMakeLists.txt w/CMakeLists.txt
index bc05ea1..1105112 100644
--- i/CMakeLists.txt
+++ w/CMakeLists.txt
@@ -145,0 +146,12 @@ elseif(WITH_DATA_STORE STREQUAL "ELOQDSS_TIKV")
+        # Both tx_service and tikv-client-c vendor Abseil and export the same
+        # absl::* target names. Add Abseil once before embedding tikv-client-c,
+        # then tell tikv-client-c not to add its second copy.
+        if(NOT TARGET absl::base)
+            if(BUILD_SHARED_LIBS)
+                set(ABSL_ENABLE_INSTALL ON CACHE INTERNAL "Install Abseil libs" FORCE)
+            else()
+                set(ABSL_ENABLE_INSTALL OFF CACHE INTERNAL "Install Abseil libs" FORCE)
+            endif()
+            add_subdirectory(tx_service/abseil-cpp "${CMAKE_BINARY_DIR}/tx_service-abseil" EXCLUDE_FROM_ALL)
+        endif()
+        set(ABSL_ROOT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/tx_service/abseil-cpp" CACHE PATH "Shared Abseil source used by tx_service and tikv-client-c" FORCE)
diff --git i/build_log_service.cmake w/build_log_service.cmake
index b41dc52..3fdb875 100644
--- i/build_log_service.cmake
+++ w/build_log_service.cmake
@@ -157,0 +158,2 @@ target_include_directories(logservice PUBLIC ${LOG_INCLUDE_DIR})
+target_compile_options(logservice PUBLIC
+    "$<$<COMPILE_LANGUAGE:CXX>:-include;gflags/gflags.h>")
diff --git i/build_tx_service.cmake w/build_tx_service.cmake
index f0ae597..5277541 100644
--- i/build_tx_service.cmake
+++ w/build_tx_service.cmake
@@ -133 +133,3 @@ endif()
-add_subdirectory(tx_service/abseil-cpp)
+if(NOT TARGET absl::base)
+    add_subdirectory(tx_service/abseil-cpp)
+endif()
